import { Inter, Open_Sans, Caveat, Nanum_Gothic  } from 'next/font/google';
 
export const inter = Inter({ subsets: ['latin'] });

export const sans = Open_Sans({ subsets: ["latin"] });
export const lusitana = Open_Sans({ subsets: ["latin"] });

export const gothic = Nanum_Gothic({
  weight: "400",
  subsets: ["latin"],
});